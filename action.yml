name: 'Account lookup'
description: 'Account lookup...'
inputs:
  accountName:
    required: false
  api-url:
    required: true
  branch:
    required: true      
  aws-region:
    required: true
outputs:
  account:  
    description: 'Account ID'
runs:
  using: 'node16'
  main: 'dist/index.js' 